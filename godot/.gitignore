# Godot 4+ specific ignores
.godot/
.import/
export.cfg
export_credentials.cfg

# Build outputs
*.tmp
exports/
builds/

# IDE
.vscode/
.idea/
*.code-workspace

# OS
.DS_Store
Thumbs.db

# Logs / backups
*.log
*.bak
*~

# Ziva plugin — 173 MB de binarios CEF/plugin, cada dev instala con
# `curl -sL https://ziva.sh/install.sh | bash` (ver README).
addons/ziva_agent/
# Output de Ziva sprite gen — no versionamos (cada dev regenera si quiere).
assets/generated/
